/**
 ******************************************************************************
 * @file    sin_form.h
 * @brief   1025 点正弦表 sin_pi_m2 的声明与定标参数
 * @details 表按 0~2π 均匀采样 1024 点(多 1 个元素使索引 1024 可直接查而不越界)，
 *          幅值 4096 = 2^12；查表值右移 sin_pi_m2_dpiybit(12) 位即归一化到 1.0。
 * @note    供 FOC 电流矢量输出模块(tb67h450.c)按电角度查表使用，索引 0~1023 对应一个电周期。
 ******************************************************************************
 */

#ifndef SIN_FORM_H
#define SIN_FORM_H /**< 包含保护宏：防止本头文件被重复包含 */

#ifdef __cplusplus
extern "C"
{
#endif

#include <stdint.h>

#define sin_pi_m2_dpix    1024 /**< 正弦表一个周期的点数(表长为该值+1)；当前工程未引用该宏，FOC 中的 1024 为硬编码 */
#define sin_pi_m2_dpiybit 12   /**< 正弦值定标位数：幅值 4096 = 2^12，查表值右移 12 位即归一化到 1.0 */

    /**
     * @brief 0~2π 正弦表
     * @note  索引 0~1023 对应一个电周期，元素定标 2^12(±4096 对应 ±1.0)；
     *        索引 1024 与索引 0 同值，使 1024 索引可直接查而不越界。
     */
    extern const int16_t sin_pi_m2[1025];

#ifdef __cplusplus
}
#endif

#endif
